// This is a small placeholder route kept to avoid router warnings.
// The real context implementation is located in `app/_context/MovieContext.tsx`.
export default function MovieContextRoute() { return null; }
